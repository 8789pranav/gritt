fastapi==0.116.1
firebase_admin==6.5.0
pydantic==2.11.7
python-dotenv==1.1.1
Requests==2.32.5
uvicorn==0.35.0
boto3==1.40.40
openai>=1.0.0
stripe>=10.0.0,<13
# Used directly by app/infrastructure/azure_pronunciation.py for the Azure
# REST call. It arrives transitively via openai, but a direct import needs
# a direct pin - a future openai release could drop it.
httpx>=0.27,<1
